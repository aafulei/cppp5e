// Exercise 19.6: Write an expression to dynamically cast a pointer to a
// Query_base to a pointer to an AndQuery (Section 15.9.1, p. 636). Test the
// cast by using objects of AndQuery and of another query type. Print a
// statement indicating whether the cast works and be sure that the output
// matches your expectations.
