// 18/01/29 = Mon

// Exercise 1.7: Compile a program that has incorrectly nested comments.

// Note: This program won't compile.

int main()
{
    /* This program won't compile, because
     * it has /* incorrectly nested comments. */
     * Now this line is taken as code rather than a comment.
     */
    return 0;
}
